.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install rlpy

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv rlpy
    $ pip install rlpy
